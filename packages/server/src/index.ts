// Phases 3-5 (IMPLEMENTATION_READINESS_CHECKLIST.md §6): HTTP routes, auth,
// sessions, the socket gateway, the per-table actor, checkpointing.
// Deliberately empty — not yet implemented.
export {};
